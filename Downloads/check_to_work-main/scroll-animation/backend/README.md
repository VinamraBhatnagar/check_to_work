# Backend

Express.js API server for the Mini Project.

## Setup

```bash
cd backend
npm install
cp .env.example .env
npm run dev
```

## API Endpoints

- `GET /api/health` — Health check

More routes to be added.
